{
  "name": "roots-and-relics",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run sync:assets && astro dev",
    "dev:offline": "N3XRA_ASSET_SYNC_OFFLINE=1 npm run dev",
    "sync:assets": "node scripts/sync-n3xra-assets.mjs",
    "build": "npm run sync:assets && astro build",
    "build:offline": "N3XRA_ASSET_SYNC_OFFLINE=1 npm run build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "astro": "^7.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
